name: Test Union Japanese Name

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run Union name test
        run: |
          python3 scripts/build_union_japanese.py
